config BR2_PACKAGE_PUREDATA
	bool "puredata"
	help
	  Pure Data (or just Pd) is an open source visual programming 
	  language for multimedia.

	  https://puredata.info/

if BR2_PACKAGE_PUREDATA 
	source "package/puredata/pd-wiringpi/Config.in"
endif
